import { Flow } from './flow';

// Gruntfile.js defines _VEXFLOW_VERSION_ and _VEXFLOW_BUILD_.
// eslint-disable-next-line
Flow.VERSION = _VEXFLOW_VERSION_;
// eslint-disable-next-line
Flow.BUILD = _VEXFLOW_BUILD_;
